import axios from 'axios';

export async function PostData({data}){

    try {
        
        const res=await axios.post('',{
            
        })

    } catch (error) {
        return error;
    }
}